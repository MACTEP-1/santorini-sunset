<properties>
    <property id="ShowSeconds" type="boolean">false</property>
    <!-- 0 = Photo, 1 = Vector - defaults to Photo since that's this face's
         actual identity now; Vector is the lighter-weight fallback. -->
    <property id="Background" type="number">0</property>
</properties>
